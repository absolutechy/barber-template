const scrollToTop = (): void => {
    window.scrollTo(0, 0)
}

export default scrollToTop
//used to scroll to the top of the page when called 
//the changes we made arrow function + const void to tell this function returns nothing 